package org.example;

import java.util.ArrayList;
import java.util.List;

public class OwnerFunction {


    /*

    public static List<Residence> residences = new ArrayList<>();

    public void addRes(){
        Residence rr = new Residence();

        rr.setResidenceID("505");
        rr.setOwnerID("111");
        rr.setLocation("warner_street");
        rr.setNumOfFloors(10);
        rr.setServices("water, electricity, wifi, heater");

        residences.add(rr);}

    public String addResidence (String string){
        addRes();
        for(int i=0; i<residences.size(); i++){
            if(residences.get(i).getResidenceID().equalsIgnoreCase(string))
                return "Residence already exists";
        }
        return "Residence added successfully";}

    public String addResidenceWithInfo (Residence resident){

        if(resident.getResidenceID()==" " || resident.getOwnerID()==" " || resident.getLocation()==" " || resident.getNumOfFloors().equals(" ") || resident.getServices() == " ")

            return "Please enter valid information. ";
        return "Resident is added successfully. ";}

   public static int search_Resident(String rr){
        for (int i=0 ; i<residences.size(); i++ ){
            if(rr.equalsIgnoreCase(residences.get(i).getResidenceID()))
                return i;
        }
        return -1;
    }




    public void addhs(){
        House hh = new House();

        hh.setId("5052");
        hh.setResidenceId("505");
        hh.setFloorId("5");
        hh.setMonthlyRent("600");
        hh.setBathroomsNum(3);
        hh.setBedroomsNum(5);


        houses.add(hh);}

    public String addHouse (String string){
        addhs();
        for(int i=0; i<houses.size(); i++){
            if(houses.get(i).getId().equalsIgnoreCase(string))
                return "The House already exists";
        }
        return "The House added successfully";}

    public String addHouseWithInfo (House house){

        if(house.getId()==" " || house.getResidenceId()==" " || house.getMonthlyRent()==" " || house.getFloorId()== " "
                || house.getBathroomNum().equals(" ") || house.getBedroomsNum().equals(" "))

            return "Please enter valid information. ";
        return "House is added successfully. ";}

    public static int search_house(String hh){
        for (int i=0 ; i<houses.size(); i++ ){
            if(hh.equalsIgnoreCase(houses.get(i).getId()))
                return i;}
        return -1;
    }



*/














}
